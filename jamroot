project
:   requirements <cflags>-std=c++11 <include>.
:   build-dir build
;

lib boost_system ;
lib pthread ;
lib stdc++ ;
lib rt ;

use-project /shmbus : shmbus ;

exe quick_test
:   quick_test.cpp
    /shmbus//shmbus
    pthread
    rt
    stdc++
;

exe benchmark
:   benchmark.cpp
    /shmbus//shmbus
    pthread
    rt
    stdc++
;

exe benchmark_unix_socket
:   benchmark_unix_socket.cpp
    boost_system
    pthread
    stdc++
;
